{
  "name": "l2-image-proceesor-module",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "npx eslint ./src || exit 0",
    "lint:fix": "npx eslint ./src --fix || exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hao Chen",
  "license": "ISC",
  "dependencies": {
    "@lnu/eslint-config": "^1.1.10"
  }
}
